import { describe, it } from "vitest";

describe.skip("createCarQuoteSubmission", () => {
  it("needs a dedicated injection seam for database mocking before it can be tested reliably", () => {
    // Intentionally skipped while the quote router submission test covers payload wiring.
  });
});
