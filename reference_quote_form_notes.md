# Quote Form Reference Notes

The provided reference uses a centered, card-based lead form with strong focus on one question or one grouped input set at a time. The visual hierarchy is simple and conversion-oriented: prominent headline, grouped inputs, then a clear next-step action.

| Area | Reference observation | Cardone adaptation |
| --- | --- | --- |
| Layout | Centered card with concentrated focus and progressive disclosure | Use a premium darker quote module with stronger contrast and cleaner step grouping inside the existing page |
| Vehicle inputs | Dependent dropdown behavior, especially Make → Model | Expand to Year → Make → Model with each field unlocking the next |
| Input grouping | Short grouped sets rather than one overwhelming long form | Keep the Cardone quote intake structured into logical subsections while still feeling fast |
| CTA rhythm | Strong single primary action at each step | Preserve one dominant submit/continue action with luxury-styled emphasis |
| Trust cues | Subtle proof and clean spacing reduce friction | Use small premium reassurance copy instead of cluttered badges |

The UI feeling to preserve is not the white-and-green palette itself, but the clarity, speed, and dependency logic. For Cardone, this should be translated into a **dark theme with metallic silver accents**, restrained glass effects, and a more upscale brokerage tone rather than a generic lead-funnel aesthetic.

The official vPIC documentation confirms two key endpoint patterns for this build. First, the API supports **GetMakesForManufacturerAndYear**, which can retrieve makes filtered by year. Second, it supports **GetModelsForMakeYear**, which can retrieve models for a specific make and model year. The documentation also notes automated traffic rate control, so the implementation should avoid wasteful repeated calls and should fetch only when the dependent field becomes active.
The NHTSA datasets page frames the vehicle-data flow as a sequence: first choose a model year, then retrieve makes for that year, then retrieve models for the selected make and year. Even though the main vPIC method list is terse, the UX implementation should follow this progressive lookup structure and cache responses per selected year and make to stay within NHTSA rate controls.
Live preview validation on 2026-04-23 shows the homepage now renders the new quote component instead of the old manual broker section. Visible fields include full name, email, phone, city, year select, make select, model select, driver age, postal code, accident history, tickets, prior insurance, and notes. The year selector is populated, while make and model remain gated until the preceding selection is made.
An interactive validation pass was started in the live preview by selecting a model year from the new year dropdown. A second anchored validation confirmed that the year field now visibly holds the selected value in the live component, which verifies that the new selector is wired into the page state correctly. After correcting the backend parser to accept the actual vPIC field names, a scripted browser validation confirmed that choosing 2026 now unlocks the make dropdown and loads 194 make options, including ACURA, ALFA ROMEO, ASTON MARTIN, and AUDI. A follow-up scripted validation then selected MERCEDES-BENZ and confirmed that the model dropdown unlocked and loaded 33 options, including AMG GT, C-Class, CLA-Class, CLE, E-Class, EQE-Class Sedan, G-Class, GLC-Class, and GLS-Class. A subsequent scripted attempt to exercise the submit action showed that the live broker section exposes the input and select fields correctly, but the submit control was not discoverable with a narrow DOM query inside the section, so final UI submission validation still requires a targeted element inspection or manual click-path confirmation. A later live-page inspection confirmed that the connector view is rendering the dynamic summary text about generating an estimate and saving into the internal database, while the explicit submit label still was not surfaced by the browser extraction layer during automated inspection. After redirecting all homepage quote CTAs to the broker connector and hiding the legacy application section, a fresh anchored preview confirmed that the page now lands directly on the dynamic quote experience; however, a lower-viewport scroll still ended around the driver-history and notes fields without exposing a rendered save button in the automation-visible layer, so final UI submission confirmation remained open until a direct DOM-driven validation was run. That final scripted browser validation populated the required fields, confirmed the estimate panel became active, revealed the two expected action buttons, and successfully triggered the save action. The live section then displayed an annual estimate of 1,980 $, a monthly estimate of 165 $, and the success message stating that the automobile quote request had been saved to the internal database.
